(ns adventure.core
  (:gen-class)
  (:require [quil.core :as q])
  (:require adventure.initial)
  (:refer adventure.initial))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
